FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
COPY scripts ./scripts
COPY evaluation ./evaluation
RUN pip install --no-cache-dir --upgrade pip && pip install --no-cache-dir "."

CMD ["python", "scripts/evaluate_api.py"]
